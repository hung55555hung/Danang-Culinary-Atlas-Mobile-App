import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  header: {
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: 10,
  },
  backArrow: {
    width: 30,
    height: 25,
    tintColor: '#fff',
  },
  title: {
    fontSize: 18,
    fontWeight: 'bold',
    color: '#fff',
    marginLeft: 5,
    flex: 1,
    textAlign: 'center',
  },
});

export default styles;
